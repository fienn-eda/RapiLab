# 데스크톱 앱으로 패키징할 때만 필요한 것. 서버로 띄우는 데에는 쓰이지 않는다.
#
# 버전을 고정하지 않는 이유: 2026-07-31 스모크에서 확인한 것은 "Python 3.14에서
# 이 스택이 설치되고 창이 뜬다"이지 특정 조합이 유일하다는 것이 아니다. 고정이
# 필요해지는 시점은 릴리스를 재현해야 할 때이고, 그것은 배포채널(③)의 일이다.
-r requirements.txt

# 네이티브 창. Windows 백엔드는 pythonnet + WebView2를 쓴다.
pywebview
# 얼리는 도구. scripts/build_app.py가 spec과 함께 호출한다.
pyinstaller
