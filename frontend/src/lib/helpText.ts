// 화면에 나오는 안내 문구를 한곳에 모은다. 컴포넌트마다 흩어 두면 문구 한 줄을
// 고치려고 그 컴포넌트를 찾아 읽어야 하는데, 문구는 코드를 몰라도 고칠 수 있는
// 것이어야 한다.
//
// 여기 오는 것은 **읽고 이해하는 문장**이다. 안내·해설·빈 상태·진행 중 안내·
// 확인 문구가 모두 그렇다. 오지 않는 것은 **조작 대상의 이름**이다 - 라벨, 버튼,
// 셀렉트 옵션, 단위 표시("초", "%"). 실패 보고는 화면에 닿는지로 갈린다 - throw되는
// 에러(예: rosterImport.ts가 형식이 아니라고 던지는 것)는 오지 않지만, 화면에 뜨는
// 경고문(가져오기 경고 셋 등)은 온다. 경계는 「문장이냐 이름이냐」이지 상태냐
// 아니냐가 아니다.
//
// `**...**`로 감싼 부분은 굵게 나온다(components/HelpText.tsx). 지원하는 표기는
// 그것 하나뿐이다 - 필요한 것이 강조뿐이고, 여기 있는 것은 유저 입력이 아니라
// 우리가 쓴 상수라 임의 HTML을 다룰 위험이 없다. `평문 전용`이라 적힌 항목은
// 문자열만 받는 자리(title 속성, window.confirm, 에러 상태, NumberField의 hint)로
// 가므로 별표가 글자 그대로 보인다.
//
// `getByText(HELP.x)`로 문구를 잡는 테스트가 있다면 주의할 것 - `**`를 넣으면 그
// 문구는 여러 DOM 노드로 쪼개져 통짜 문자열로는 더 안 잡힌다. 문구를 바꾸지 않고
// 강조만 추가해도 그 테스트는 빨개진다.
//
// 값이 끼는 문구는 함수다. 자리표시자 문자열을 쓰지 않는 이유는 조용히 틀리기
// 때문이다 - 이름을 오타 내도 컴파일러가 말이 없고 화면에 자리표시자가 박힌다.
// 포맷 헬퍼(percent, formatDamage)를 통과한 값은 문자열로 받고, 그 밖의 값은
// 숫자로 받아 자릿수와 단위를 문구가 붙인다. 인자 순서는 문장 자신의 어순을
// 따른다 - `gainAt(current, threshold, gapPoints)`와
// `keepAbove(threshold, current, slackPoints)`가 같은 두 값을 반대 순서로 받는
// 것은 그래서다.

/** 도움말 단계에 붙는 스크린샷. blablalink의 공유 아이콘과 "링크 복사하기"는
 * 둘 다 라벨이 없어, 글만으로는 나란히 있는 방패/물음표 아이콘과 구별해 지목할
 * 방법이 없다. */
interface HelpShot {
  /** `public/help/` 아래의 파일 이름. */
  src: string
  alt: string
  width: number
  height: number
}

interface HelpStep {
  text: string
  shot?: HelpShot
}

export const HELP = {
  app: {
    subtitle: 'Shiftypad 데이터 기반 덱 빌더 & 계산기',
    /**
     * 톡톡이는 2026-08-19부터 일부 니케에 반영된다(엔진의
     * `MANUAL_TAP_FIRE_INTERVAL`). 그래서 「미구현」이라고 적으면 거짓인데,
     * 여기서 유닛 이름을 세면 표에 유닛이 추가되는 날 낡는다 — 누가 해당되는지는
     * 덱 결과가 유닛별로 말하므로 그쪽을 가리킨다.
     */
    cubeAssumption:
      '재장전 큐브 15레벨 장착, 버스트 사이클이 밀리지 않은 환경으로 가정해요. 톡톡이는 일부 니케만 반영돼 있고(해당하면 덱 결과에 표시돼요), 버충은 미구현 상태예요.',
    noProfiles: '아직 동기화된 계정이 없어요. 위에서 blablalink 동기화를 시작해보세요.',
  },

  boss: {
    rotationPicker:
      '회차 보스를 고르면 **약점 속성과 적정거리**가 채워지고(+코어 설정), **나머지 보스 설정은 전부 기본값으로 되돌아가요.**',
    rangeBand:
      '적정거리에서 쏘는 무기는 평타 대미지가 올라가요. 런처(RL)는 어느 거리에서도 받지 않아요.',
    coreHittable:
      '보스에 코어가 있을 때 체크. 코어 지름을 비워 두면 **모든 평타가 코어에 명중한다고 가정**하고, 평타 비중이 큰 유닛이 실제보다 높게 평가될 수 있어요.',
    coreDiameter:
      '코어와 **탄착군의 면적비**가 평타의 코어 명중률을 정해요. 이 값이 있어야 명중 스탯이 실제로 계산에 반영되고 **비워 두면 코어히트율 100%로 계산해요**.',
    coreMeasure:
      '스크린샷에서 **코어와 탄착군의 지름을 픽셀로** 재서 넣으면 엔진 단위로 바꿔줘요. **비율**만 쓰기 때문에 창 크기·해상도·전체화면 여부는 상관없어요. 대신 두 가지를 지켜야 해요 — 탄착군은 **명중 버프가 없는 상태**여야 하고, 두 값이 **같은 창 설정**에서 나와야 해요. 탄착군은 클수록 정확하니 **SG가 가장 좋은 기준자**예요.',
    corePierce:
      '코어와 본체가 **별개 객체**로 만들어진 보스예요. 이 옵션을 체크하면 코어 타격 가능 옵션이 자동으로 켜져요.',
    partDestructible:
      '파츠파괴에 의존하는 유닛(예: 아크레인저 블랙)의 최대 잠재력 모델을 선택해요. 체크 해제 시 하한 모델을 사용해요.',
    spawnsAdds:
      '잡몹이 주기적으로 생성되는 보스예요. **애미하라 택틱**을 기본적으로 후보에서 제외 — 「애미하라 택틱」으로 켜세요.',
    holdFireDespiteAdds:
      '잡몹이 나와도 **평타를 안 쏠 수 있다**고 보고 애미하라 택틱을 다시 후보에 넣어요. 계산결과는 미하라 지딜이 잡몹에 새지 않았을 때의 값이에요.',
    partDestructionTimes:
      '파츠가 **실제로 깨지는 시각**을 초로 적어요(예: `1, 61, 126`). 파괴에 반응하는 스킬(레이븐의 싱글 포인트 어택, 디젤:윈터 스위츠의 지속 대미지)이 **그 시각마다 자기 지속시간만큼만** 걸려요. 비워 두면 언제 깨지는지 모른다고 보고 지금까지의 근사로 계산해요.',
    elementalInterrupt:
      '**약점 속성 니케가 덱마다 최소 1기** 필요해요.',
  },

  charge: {
    cube: '렐릭 베어는 재장전 속도, 택티컬 베어는 **10발마다 3발 충전**이에요. 충전 카운터는 **풀버 시작시 0부터 센다고 가정**해요.',
    speedTotal:
      '동기화된 로스터는 부위별 값이 아니라 합계만 알고 있어요. 차지속도는 **같은 수치끼리 합산 후 반올림**돼요.',
    maxAmmo:
      '풀버스트 진입 시 **탄창은 가득**으로 가정해요.',
    notes: '이 계산에서 판정된 것들이에요',
    ladderGap: (gapPoints: number) => `다음 구간까지 ${gapPoints.toFixed(2)}%p 남았어요.`,
    ladderChargeGone: '더 올릴 구간이 없습니다',
    ladderCeiling: (ceiling: string) =>
      `오버로드 상한 ${ceiling}까지만 보여줍니다 — 4부위 전부 최고 수치일 때의 값이에요.`,
    ladderAtLast: '더 올릴 구간이 없습니다 — 지금 합계가 이미 최고 구간입니다.',
    /** 상한이 아니라 답이 먼저 멈춘 경우. 오버로드는 아직 남았는데 살 이유가
     * 없다 — 대개 탄창이 타수를 막고 있을 때다. */
    ladderAnswerSettled:
      '여기서부터는 더 올려도 결과가 같습니다 — 타수도 확률도 바뀌지 않아요.',
    /** 평문 전용 - NumberField의 hint는 HelpText를 거치지 않고 그대로 렌더해 별표가
     * 글자 그대로 나온다. */
    rosterFallbackHint: '(%, 비우면 동기화된 로스터 값)',
  },

  miranda: {
    notInRoster: '미란다가 로스터에 없어요. 동기화 탭에서 로스터를 다시 가져와 주세요.',
    intro:
      '미란다는 고정이고, 네 자리를 채우면 **파워업!**과 **웨이크업! 크확**을 누가 받는지 알려줘요.',
    running: '시뮬레이션을 돌리는 중이에요 — 몇 초 걸려요.',
    gainCapped: (cap: string) => `오버로드 공격력을 상한(${cap})까지 올려도 매 사이클 받지는 못해요`,
    gainAt: (current: string, threshold: string, gapPoints: number) =>
      `오버로드 공격력 ${current} → ${threshold}면 매 사이클 받아요 (+${gapPoints.toFixed(2)}%p)`,
    keepAlways: '오버로드 공격력이 없어도 매 사이클 유지돼요',
    keepAbove: (threshold: string, current: string, slackPoints: number) =>
      `오버로드 공격력이 ${threshold} 밑으로 내려가면 매 사이클은 못 받아요 (지금 ${current}, 여유 ${slackPoints.toFixed(2)}%p)`,
    targetsChange: (cycles: string) => `⚠ ${cycles}사이클에는 파워업!을 받는 니케가 달라요`,
    burstsFewer: (total: number, bursts: number) =>
      `⚠ 미란다는 ${total}사이클 중 ${bursts}번만 버스트해요 (같은 1버스트에 니케가 둘이에요)`,
  },

  recommend: {
    minRoster: (minimum: number) => `덱을 추천하려면 준비된 니케가 최소 ${minimum}기 필요해요.`,
    /** modeLabel은 RecommendPanel.tsx가 넘긴다 - 제출 버튼의 진행 중 라벨과 같은
     * 문구라 라벨이고, 라벨은 이 파일로 오지 않는다. */
    searchRunning: (modeLabel: string) =>
      `${modeLabel} — 수천 번의 시뮬레이션을 실행하며 보통 2~5분이 걸려요. 아직 진행 중이니 완료되면 버튼이 다시 활성화돼요.`,
    evaluateRunning: '기대 딜량 계산 중이에요 — 몇 초면 끝나요.',
    poolNote: (included: number, total: number) =>
      `${included}/${total} 탐색 풀에 포함됨 — 사용하지 않을 니케는 니케 풀 탭에서 설정해요`,
    /** 앞 공백이 있다 - 위 문장 뒤에 이어 붙는 꼬리다. */
    poolUnsupported: (count: number) => ` (보유 중이지만 아직 엔진 미지원 ${count}기)`,
  },

  recommendMode: {
    single: '기대 딜량이 높은 개별 덱을 찾아줘요',
    raid: '설정한 덱 개수만큼 최적화해요',
    draft: '직접 편성한 니케들을 기반으로 나머지 자리를 최적화해요',
    evaluate: '직접 짠 덱의 기대 딜량만 빠르게 계산해요, 최적화는 하지 않아요',
  },

  draft: {
    /** 끝 공백이 있다 - seatHintCommon이 뒤에 이어 붙는다. 덱 사이 이동·맞바꾸기는
     * 여기에만 있다: 덱이 하나인 화면에서는 둘 다 같은 덱 안이라 아무 일도
     * 일어나지 않는다. */
    seatHintWithDeckPick:
      '니케를 누르면 선택중인 덱(빨간 테두리)에 들어가고, 그 덱이 차면 다음 덱으로 넘어가요. 덱을 누르면 활성 덱이 바뀌어요. 편성된 니케를 클릭하고 다른 덱을 누르면 옮기거나 맞바꿔요. ',
    /** 끝 공백이 있다 - seatHintCommon이 뒤에 이어 붙는다. */
    seatHintSingleDeck: '니케를 누르면 덱에 추가돼요. ',
    seatHintCommon:
      '덱에 편성된 니케를 클릭하면 선택 상태로 바뀌어요. 같은 자리를 다시 누르면 편성에서 빠져요. Esc로 취소해요. 버스트 순서는 엔진이 정해요.',
    tiersIntro:
      '세 단계로 올라가요: 제출한 초안, 초안의 유닛만으로 만든 최선의 배분, 벤치까지 포함한 추천.',
    /** 제외는 계정 단위 결정이라 니케 풀 탭에서만 풀린다. 이 화면에서 누르면
     * 아무 일도 안 일어나는데, 그 침묵이 「칩이 고장 났다」로 읽힌다. */
    excludedElsewhere: '다시 활성화하려면 니케 풀에서 활성화해주세요',
  },

  results: {
    emptyDecks: '아직 추천된 덱이 없어요.',
    emptyRaid: '아직 배분된 레이드 덱이 없어요.',
    raidSplit: (deckCount: number) =>
      `이 ${deckCount}개 덱을 편성하세요.`,
    bench: (names: string) => `벤치 (덱에 배정되지 않음): ${names}`,
    /**
     * 이 목록은 편성 자리가 아니라 **버스트를 쓰는 순서**다. 게임에서 자리와
     * 버스트 순서는 별개 축이라(자리는 편성 화면에서, 버스트는 탭으로 정한다)
     * 이걸 「자리 순서」라고 부르면 좌석 안내와 화면에서 충돌한다.
     */
    burstOrder:
      '나열된 순서는 버스트를 쓰는 순서예요 — 엔진이 기대 딜량이 가장 높게 나오도록 고른 거예요.',
    swapCutoff: '탐색이 상한에 걸려 끝까지 진행하지 못했어요. 더 나은 조합이 남아 있을 수 있어요.',
    holdBurst: (names: string) =>
      `${names}는 첫 풀버스트에 버스트를 아껴주세요 — 그래야 이 수치대로 나와요.`,
    holdFire: (names: string) =>
      `${names}는 자기 버스트 사이클에서 **평타를 쏘지 마세요**`,
    /**
     * 톡톡이는 차지가 다 차기 전에 놓아 쏘는 조작이다. **언제 하는지가 덱마다 갈려서**
     * 문구가 셋이다 — `partial_charge_slugs`와 `partial_charge_full_rounds` 둘의
     * 조합으로 갈린다.
     *
     * 먼저 가르는 것은 `partial_charge_slugs`, 즉 **배율을 실제로 버렸는가**다. 차속
     * 버프가 세게 걸린 구간에서는 차지가 0프레임까지 내려가므로(앨리스: 버스트
     * 80.15% + 스킬1의 캐스터 기준 0.17505초) 거기서는 눌러도 이미 풀차지라 손은
     * 톡톡이인데 잃는 것이 없다 — 슬러그가 없으면 그 구간뿐이니 `burstOnly`다.
     *
     * 슬러그가 있으면(배율을 실제로 버렸으면) 나머지는 `partial_charge_full_rounds`가
     * 가른다. 그 매거진에 톡톡이가 든 채로 풀차지가 하나도 없으면(값이 0) 매거진을
     * 통째로 톡톡이로 쏘는 것이니 `always`, 1 이상이면 매거진 안에서 풀차지와
     * **섞은** 것이니 `tapFireMixed`다(밀크: 블루밍 바니 — 그녀의 풀차지는 Pierce
     * 6초를 되살리므로 「항상 톡톡이」는 거짓이고 그대로 두면 플레이어가 관통특화를
     * 잃는다).
     *
     * **이유는 안 적는다**(Fienn, 2026-08-19) — 화면은 「무엇을 하라」까지만 말한다.
     */
    tapFireAlways: (names: string) => `${names}는 항상 톡톡이로 계산했어요.`,
    tapFireBurstOnly: (names: string) =>
      `${names}는 버스트 턴은 톡톡이, 아닐 때는 톡톡이 X 로 계산했어요.`,
    tapFireMixed: (name: string, fullRounds: number) =>
      `${name} 풀차지 ${fullRounds}회 + 톡톡이로 계산했어요.`,
    /**
     * 「자신과 양 옆 아군 2명」을 대상으로 하는 버프(루주의 Sword Coin, 플로라
     * 애장품의 Peace of Mind)를 위한 편성 지시. `seats`가 좁을 때만 자리 번호를
     * 넘긴다 — 「양 옆에 둘」은 앞열 3번 자리로도 만족되는데 거기서는 루주의
     * 버프가 아예 안 켜지므로, 자리 조건이 있는 유닛은 그것까지 말해야 한다.
     */
    seating: (caster: string, allies: string, seats: string | null) =>
      seats === null
        ? `${caster} 양 옆에 ${allies}를 앉혀주세요 — 그래야 이 수치대로 나와요.`
        : `${caster}는 ${seats}번 자리 중 한 곳에 두고, 양 옆에 ${allies}를 앉혀주세요 — 그래야 이 수치대로 나와요.`,
    /** 평문 전용 - title 속성이라 별표가 글자 그대로 나온다. */
    pinTitle: '고정해서 여기 유지돼요',
    /**
     * **버충 밀림** — 화면은 **시간을 앞에** 둔다. 판별하는 값이 시간이기
     * 때문이다: 덱 1과 덱 3은 똑같이 14사이클 중 11이 밀리는데 합계가 4.3초 대
     * 11.6초이고, 판독은 그 둘을 「안 밀림」과 「밀림」으로 가른다. 문턱은 두지
     * 않는다 — 시간을 보여주면 사람이 스스로 정한다.
     *
     * 「게이지가 병목인 시간」이라고 적으면 안 된다: 게이지와 쿨타임이 같은
     * 시각인 사이클은 병목이지만 아무것도 안 밀렸고, 엔진은 그것을 안 센다.
     *
     * 평문 전용 - title 속성이라 별표가 글자 그대로 나온다.
     */
    gaugeDelayTitle: '쿨타임은 돌았는데 게이지가 안 차서 버스트가 밀린 총 시간',
  },

  savedRuns: {
    restoreHint: '이전 시뮬레이션의 결과를 다시 볼 수 있어요',
    empty: '저장한 결과가 없어요.',
    /** 평문 전용 - window.confirm이라 별표가 글자 그대로 나온다. */
    confirmDelete: (name: string) => `"${name}"을(를) 삭제할까요?`,
    capReached: (cap: number) =>
      `보관은 ${cap}개까지예요. 목록에서 몇 개를 지우고 다시 저장해주세요.`,
  },

  draftActions: {
    /** 평문 전용 - window.confirm이라 별표가 글자 그대로 나온다. */
    confirmClear: '모든 덱을 초기화하시겠습니까?',
    /** 평문 전용 - 같은 이유. */
    confirmOverwrite: '지금 편성을 저장한 결과로 바꿀까요?',
    /** 가져오기·폼 채우기·계정 복원 셋이 같이 쓴다 - 어느 쪽으로 들어온
     * 편성인지는 말하지 않는다. */
    droppedUnits: (count: number) =>
      `되돌린 편성에서 ${count}기가 빠졌어요 — 지금 로스터에 없거나 미사용으로 둔 니케예요.`,
  },

  roster: {
    importMalformedUnit: '유닛에 name_en/raid400이 없어요',
    emptyFilter: '조건에 맞는 니케가 없어요.',
    /** 합계는 롤로 되돌릴 수 없다 - 부위별로 보여주려면 동기화가 롤을 실어야
     * 한다. 왜 이 니케만 격자가 아닌지 말해주지 않으면 고장으로 읽힌다. */
    gearNeedsResync: '부위별로 보려면 로스터를 **다시 동기화**해 주세요',
    /** 격자의 빨간 수치와 흰 행이 무슨 뜻인지는 화면 어디에도 안 적혀 있다.
     * 인게임 라벨 색(블루/블랙)으로 부르는 것은 Fienn의 표현이다 - 게임에서
     * 그 이름으로 보이는 것을 우리 단계 번호와 이어 주는 것이 이 문장의 일이다. */
    gearTierLegend:
      '12~14단계(블루라벨) 옵션은 **빨간색 수치**, 15단계(블랙라벨) 옵션은 ' +
      '**흰색 바탕 + 빨간색 수치**로 표현해요.',
    importUnsupported: (count: number, names: string) =>
      `보유 유닛 중 ${count}기가 아직 미지원이라 추천에서 제외돼요: ${names}`,
    importUnmeasured: (count: number, names: string) =>
      `보유 유닛 중 ${count}기가 제외됐어요 — 레벨 400 스탯이 측정된 적이 없어요: ${names}`,
  },

  profile: {
    /** 평문 전용 - window.confirm이라 별표가 글자 그대로 나온다. */
    confirmDelete: (label: string) =>
      `"${label}" 프로필을 삭제할까요? 동기화된 로스터와 캐시된 결과가 함께 삭제돼요.`,
  },

  sync: {
    /** 북마크릿 주소를 손에 쥔 순간에만 필요한 안내. 전체 절차는 도움말이
     * 따로 말하고, 이쪽은 바로 옆 버튼이 무엇인지 설명한다. */
    bookmarkletHint:
      '① 아래 버튼으로 주소를 복사해요. ② 브라우저에서 북마크를 새로 만들고 **주소(URL) 칸에 붙여넣어요**. ③ blablalink에 로그인한 상태로 그 북마크를 눌러요. RapiLab을 켜 둔 채로 누르면 로스터가 바로 들어와요.',
    copied: '복사했어요. 브라우저에서 북마크를 만들고 주소 칸에 붙여넣어 주세요.',
    copyBlocked: '자동 복사가 막혀 있어요. 아래 주소를 직접 복사해 주세요.',
    chooseServer: '어느 서버의 계정을 가져올까요?',
    serverChoice: '계정이 있는 서버를 고르면 그 서버만 조회해요. 모르겠으면 자동으로 두세요.',
    importing: '가져오는 중…',
    nameUnavailable:
      '계정 이름을 읽지 못했어요. 로스터는 정상이에요. 위 계정 드롭다운 옆 계정 이름 바꾸기로 원하는 이름을 붙여주세요.',
    /** 유니온 탭이 계산을 막을 때. 동기화만 다시 해서는 안 풀린다 - 이미 설치된
     * 북마크릿은 설치 시점 소스가 박제된 것이라 싱크로 레벨을 보내지 않는다. */
    unionNeedsActualStats:
      '유니온 레이드는 싱크로 레벨로 싸워요. 지금 로스터에는 그 레벨의 스탯이 없어서 계산할 수 없어요. "동기화 방법"을 열어 북마크릿을 다시 설치한 뒤, 로스터를 다시 동기화해 주세요.',
    /** 평문 전용 - error 상태로 가고 그 자리는 다른 에러도 쓰므로 HelpText로 안 그린다. */
    bookmarkletNoAccount:
      '북마크릿이 계정 정보를 보내지 않았어요. "동기화 방법"을 열어 북마크릿을 다시 설치한 뒤 시도해 주세요.',
  },

  syncHelp: {
    steps: [
      {
        text: 'blablalink **SHIFTYPAD** 페이지 우측 상단의 공유 아이콘을 눌러요.',
        shot: {
          src: 'shiftypad-share-button.png',
          alt: 'SHIFTYPAD 페이지 우측 상단, 방패와 물음표 아이콘 오른쪽에 있는 공유 아이콘',
          width: 748,
          height: 193,
        },
      },
      {
        text: '**링크 복사하기**를 눌러 URL을 복사해요.',
        shot: {
          src: 'shiftypad-copy-link.png',
          alt: '공유하기 창에서 Facebook·Twitter·Naver·whatsApp 오른쪽에 있는 링크 복사하기 버튼',
          width: 626,
          height: 211,
        },
      },
      { text: '복사한 URL을 아래 칸에 붙여넣어요.' },
      { text: '나타나는 **북마크릿 주소 복사** 버튼을 눌러요.' },
      { text: '브라우저에서 북마크를 새로 만들고, **주소(URL) 칸에 붙여넣어요**.' },
      {
        text: 'RapiLab을 켜 둔 채, blablalink에 로그인한 상태로 그 북마크를 눌러요. 로스터가 이 화면으로 바로 들어와요.',
      },
      {
        text: '계정 이름이 UID로 보이면 위 **계정** 드롭다운 옆에서 [이름 바꾸기]를 이용해 수정할 수 있어요. 실제 닉네임이 아니어도 괜찮아요. blablalink가 이름 조회만 자주 막아서, 이름은 붙을 때도 있고 안 붙을 때도 있어요.',
      },
    ] as readonly HelpStep[],

    resyncHeading: '다시 동기화할 때',
    resync:
      '북마크만 다시 누르면 돼요. 공유 URL을 또 붙여넣을 필요는 없어요. RapiLab이 켜져 있어야 해요.',

    multiAccountHeading: '계정이 여러 개일 때',
    multiAccount: '계정마다 북마크가 따로 필요해요 — 북마크릿에는 그 계정의 ID가 들어 있어요.',
    multiAccountSteps: [
      '추가할 계정의 SHIFTYPAD에서 공유 URL을 복사해 위 1~5단계를 다시 해요.',
      '북마크 이름을 계정 이름으로 바꿔두면 어느 계정 것인지 헷갈리지 않아요.',
      '**그 계정으로 blablalink에 로그인한 상태에서** 해당 북마크를 눌러요.',
    ],
    switchNote:
      '동기화한 계정은 위쪽 **계정** 드롭다운에서 전환해요. 계정별 로스터와 추천 결과는 섞이지 않아요.',
  },

  /** 화면 맨 아래에 늘 떠 있는 출처 고지. README와 같은 문장이다 - 앱과 저장소
   * 어느 쪽을 먼저 본 사람이든 같은 말을 읽어야 한다.
   *
   * 여기가 말하는 것은 무관함이지 적법성이 아니다. "약관을 위반하지 않았다" 같은
   * 자기 판단은 이해당사자가 남의 계약에 내린 결론이라 무게가 없고, 약관이나
   * 해석이 바뀌면 그때부터는 유저에게 한 허위 진술이 된다. */
  attribution: [
    '이 프로젝트는 SHIFT UP · Level Infinite과 무관한 팬 제작물입니다.',
    '게임 내 명칭과 자산의 권리는 원저작자에게 있습니다.',
  ],

  /** 화면 맨 아래에서 펼쳐 보는 개인정보 안내. 여기 적힌 것은 전부 코드에서
   * 확인한 사실이라, 코드가 바뀌면 이 문구도 같이 바뀌어야 한다 - 특히
   * 「밖으로 나가는 것」은 새 바깥 요청이 생기는 순간 틀린 말이 된다. */
  privacy: {
    updated: '2026-08-06',
    lede:
      '**RapiLab에는 서버가 없어요.** 로스터도 계산도 전부 이 PC 안에서 이뤄지고, 만든 사람에게 전송되는 정보는 없어요. 그래서 제3자 제공이나 보관 기간 같은 항목이 성립하지 않아요.',
    sections: [
      {
        heading: '이 PC에 저장하는 것',
        items: [
          'blablalink 계정 식별자(open_id)와 서버, 닉네임 — 계정마다 로스터를 따로 두기 위해 필요해요.',
          '로스터(보유 니케와 투자 상태), 추천 결과 캐시, 직접 보관한 실행 결과.',
          '익명 ID — 앱을 처음 열 때 만드는 무작위 값이에요. 게임 계정과 무관하고, 이 PC 밖으로 나가지 않아요.',
          '저장 위치는 앱 안의 브라우저 저장소 한 곳이에요 — **%LOCALAPPDATA%\\RapiLab** 폴더 아래요. 로스터나 계정 정보가 담긴 기록은 그 밖 어디에도 남지 않아요.',
        ],
      },
      {
        heading: '밖으로 나가는 것',
        items: [
          '앱이 스스로 보내는 요청은 **시작할 때 GitHub에 새 버전이 있는지 묻는 것 하나**뿐이에요. 로그인하지 않고, 계정 정보도 싣지 않아요. 다만 웹 요청이라 GitHub은 접속한 IP를 알게 돼요.',
          'blablalink 조회는 앱이 아니라 **당신의 브라우저**가 당신의 로그인 세션으로 직접 해요. 북마크릿에 들어가는 것은 공유 URL에서 읽은 open_id 하나뿐이에요.',
          '로스터를 조립하는 요청도 이 PC 안의 주소로만 가요.',
        ],
      },
      {
        heading: '하지 않는 것',
        items: [
          '**비밀번호나 로그인 정보를 저장하지 않아요.** 요구하지도 않아요 — 이 앱에는 로그인이 없어요.',
          '**게임에 접속하지 않아요.** 앱은 게임 클라이언트도 게임 서버도 건드리지 않아요.',
          '**남의 계정을 조회하지 않아요.** 북마크릿에는 당신이 만든 공유 URL의 open_id 하나만 들어가고, 그 하나로만 조회해요.',
          '**혼자 무언가를 반복하지 않아요.** 동기화는 당신이 북마크를 누를 때만 일어나고, 그때 당신 계정의 서버들만 확인해요.',
        ],
      },
      {
        heading: '지우는 방법',
        items: [
          '계정 하나만 지우려면 위쪽 **계정** 드롭다운에서 그 프로필을 삭제해요.',
          '전부 지우려면 앱을 끄고 **%LOCALAPPDATA%\\RapiLab** 폴더를 지우면 돼요. 남는 것이 없어요.',
        ],
      },
    ],
    closing:
      '문의나 지적은 GitHub 저장소(fienn-eda/RapiLab)로 남겨주세요. 이 안내가 바뀌면 앱 업데이트와 함께 반영돼요.',
  },
} as const
